# KuVibe 0.3.2 规则与工作流刷新

时间：`2026-09-15T23:15:35+08:00`
变更集：`kuvibe-refresh-0.2.0-to-0.3.2`。

## 问题与上下文

用户要求按本地 `kuVibe.md` 最新内容升级规则。协议目标为 0.3.2 / schema 2，安装状态为 0.2.0 / schema 2，因此执行同 schema 刷新。不需要网络协议替换或结构迁移。

## 决策、归属与保留要求

- 合并 AGENTS 托管区和四个工作流，保留 Swagger、薄控制器、兼容性、缓存审查和活动里程碑等项目要求。工作流含项目定制，标为 `ownership=mixed`，以后继续合并。
- requirement/development 应用 revision 2；router/review/documentation 保持协议指定 revision 1，本地语言适配不伪造上游 revision。
- `.agents/project.md` 仅追加语言与版本约定。原创文档支持简体中文；已有英文提交历史作为提交说明例外，历史文档不翻译。
- 项目上下文原文、stack、已有 active/implemented 笔记、docs、源码、清单、锁文件及用户未跟踪的 `packages/backend/src/migrations/1789485191508-update-table.ts` 均保留。
- 替代方案：重新初始化会损失项目知识；结构迁移没有 schema 依据，均不采用。使用 Git 历史恢复，不创建仓库备份目录。

## 实现与版本决定

- 补齐版本优先检查、语言复用、风险路由、需求完成门禁及 §30.1 的版本影响、同步和重试规则。
- 安装状态基线/目标：`0.2.0 → 0.3.2`；schema `2 → 2`。这是应用已发布协议的兼容规则刷新，工作流能力语义影响为 minor；有效安装目标直接取本地协议 0.3.2，不从安装基线计算新的协议 release。
- 版本来源：仅 `.agents/kuvibe.yaml`；`initializedAt` 保留 `2026-09-07T12:53:58+08:00`，候选验证通过后更新 release、已应用 revision 和 `lastUpdatedAt`。
- 产品版本同步及产品 CHANGELOG：N/A。本次仅更新开发规则，没有产品运行时/API 发布变化；根 `1.0.0`、私有应用 `0.0.1` 保持原值。现有产品开发期版本延后证据和后续发布政策待定项记录在项目上下文；不将 KuVibe 版本赋给产品。
- 同一变更集重试沿用上述基线/目标及本笔记，不重复递增或创建重复记录。

## 验证与审查

- 候选检查通过：必需上下文可读、router 路径存在、托管区唯一且成对、四个工作流 revision 与目标协议一致。
- SHA-256 基线检查确认原有 docs、笔记、源码、清单、锁文件、stack 和协议文件未变化；项目上下文原文作为完整前缀保留。
- `git diff --check` 通过。范围仅为 Markdown 和 KuVibe 状态，无需应用运行时测试或数据库操作。
- 最终状态写入后复核协议/状态版本、schema、模板 revision、初始化时间和日期格式，并再次检查差异及保留范围。

## 文档影响、后果与后续事项

开发规则和上下文已更新；业务规则、数据模型、API/CLI、UI、产品运维及架构文档影响均 N/A。Vben 的 M6/M8 等未完成范围继续保留，不将本次刷新视为业务里程碑完成。未来需求使用持久化语言与版本约定；本次无提交、标签或发布。

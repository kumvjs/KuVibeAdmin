# 变更记录

## 1.2.2 — 2026-09-18

- 将附件与上传策略五项权限统一归入“系统管理 → 附件管理”二级目录，不再直接挂系统管理根节点。
- setup 支持旧版五项权限原地调整父级，保留权限 ID、角色关联和元数据；非预期父级冲突仍回滚。
- 隔离数据库验证、类型检查及构建通过；根与 core 统一 patch：1.2.1 → 1.2.2。

## 1.2.1 — 2026-09-18

- 修复初始化数据缺失：补齐根部门、普通默认角色及系统管理下的部门、菜单、角色、用户页面和 21 个管理权限；附件与上传策略只登记权限节点。
- 重复运行 setup 可补齐基础数据，保留已有超级用户、密钥及自定义菜单元数据；冲突回滚，不恢复停用或软删除记录。
- 清理 user 角色已有管理授权，保留业务权限及其他角色授权，提交后定向清理权限缓存；在停服维护窗口运行。
- 新增隔离 PostgreSQL 初始化验证与缓存/脚本回归测试，更新初始化和 Vben 接入说明。
- v2 仅形成站内信等生产能力规划，尚未实现，不提前升至 2.x。根目录与 core 统一 patch：1.2.0 → 1.2.1。

## 1.2.0 — 2026-09-18

- 正式更名为 KuVibeAdmin，仓库地址调整为 `kumvjs/KuVibeAdmin`，新增根 README，明确基于 KuVibe 工程协议及当前仅交付 NestJS 后端的范围。
- 新增根目录启动、构建、初始化、类型检查、测试、迁移及文档命令；修正 production 构建入口和 ESM 测试调用。
- 完成 M8 发布验收：真实应用/Passport、PostgreSQL/Redis、全部迁移往返、权限缓存失效、并发刷新及固定 Vben v5.7.0 浏览器联调；保留可重复测试脚本。
- 修复 DTO 类未提交的 undefined 字段被当作显式写入的问题，保留局部更新与 null 清空语义。
- 修正 POST 成功响应的 OpenAPI 状态码和退出响应，保持实际 HTTP 行为及 ResOp 契约不变；更新迁移与接入文档。
- 统一版本 minor：根目录与 core 从 1.1.1 升至 1.2.0。数据库/schema/Redis 键/API 前缀不随品牌改变，保留已应用时间戳迁移。
- 验证边界：全量 ESLint 有既存问题，本次改动文件通过；Atlas Pro lint、生产规模及零停机演练未执行。Vben 前端不随本版本发布，其用户表单仍需接入方适配。

## 1.1.1 — 2026-09-17

- 接入 Atlas 项目 Skill 与 Agent 迁移路由，明确生成后的数据保留修正、隔离数据库验证、锁与发布兼容性检查；普通终端 pnpm 不自动运行 Skill。
- 在快速开始中统一迁移使用入口，提供生成新迁移、审查已有迁移的可复制提示词和共同交付标准。
- 修正未执行的时间类型迁移：13 表 40 列按确认的 UTC 历史语义原地转换，保留软删除、令牌过期时间、约束及索引，支持相同语义的回滚。
- 增加 PostgreSQL 17 带数据 up/down/up 验证，覆盖非 UTC 会话、微秒、NULL、默认值及实体结构一致性。
- 版本影响 patch，根目录与 core 统一从 1.1.0 递增至 1.1.1；Atlas CLI/Pro lint 和生产规模演练未执行。

## 1.1.0 — 2026-09-17

- 完成 M6 用户展示时区偏好：公开 IANA 选项、登录用户偏好读写，null 表示跟随设备；管理员写入入口使用同一校验。
- 提供统一 Day.js 工具：严格时间点解析、时区展示、业务日期与半开日期范围；显式传入业务时区，覆盖夏令时，异常零点拒绝静默调整。
- 创建/更新/软删除/令牌过期时间显式采用 timestamptz 默认精度；PostgreSQL 池连接统一 UTC。按开发阶段约定不生成或执行历史数据迁移。
- 增加 Swagger 可空契约、HTTP/服务/工具测试、隔离 PostgreSQL 与跨进程 TZ 验证及前端接入文档；业务权益与旅行切换规则留给业务实现。
- 版本影响 minor，根目录与 core 统一从 1.0.1 递增至 1.1.0。

## 1.0.1 — 2026-09-17

- 将 KuVibe 安装规则从 0.3.2 刷新至 0.3.3，需求与开发工作流应用 revision 3，schema 保持 2。
- 修正无依据的默认 deferred 约定：版本延后必须有可追溯政策、自动化或用户决定及最终递增触发条件。
- 完成总结必须报告版本语义影响、执行结果、旧版本 → 新版本及理由。
- 按用户确认，根目录和 core 使用统一版本并同步递增。本次为兼容规则修正，patch：1.0.0 → 1.0.1；不追补历史需求，也不包含尚未完成的 DTO 改动。

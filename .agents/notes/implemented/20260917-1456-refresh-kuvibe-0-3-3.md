# KuVibe 0.3.3 刷新与版本约定纠正

时间：`2026-09-17T14:56:14+08:00`
变更集：`kuvibe-refresh-0.3.2-to-0.3.3`。

## 问题与上下文

用户要求按本地 `kuVibe.md` 0.3.3 升级规则。原安装为 0.3.2/schema 2；此前刷新错误地将缺少发布脚本、CHANGELOG 和历史递增当成默认 deferred 的依据，后续需求据此推迟版本变更。

## 决策、证据与替代方案

- 按协议 §4.4 和 §30.1 窄范围撤销无依据例外，保留历史笔记，不重新生成项目知识。
- 读取实际清单，修正项目上下文 core 0.0.1 的过期快照；刷新开始时根/core 均为 1.0.0。
- 用户在本次会话明确选择“统一版本：根目录与 core 同步递增”，据此将根清单定为权威，core 同步。不存在发布延后指示。
- 不再依据相同版本猜测统一政策，也不以缺少发布流程为延后理由；版本范围歧义经聚焦问题解决。无需 schema 迁移，不追补旧需求版本。

## 实现与版本决定

- mixed requirement/development 工作流 revision 2 → 3：补齐权威清单、例外证据、歧义 pending、实际递增及用户可见版本报告分支，保留项目原有工程要求。
- 产品变更语义/有效影响：patch，兼容的规则和文档修正。执行结果 bumped：根 `package.json` 与 `packages/backend/package.json` 均 `1.0.0 → 1.0.1`，新增 CHANGELOG；docs 无版本。版本决定在写入前记录于本次 active 计划。
- 安装状态 bumped：`.agents/kuvibe.yaml` `0.3.2 → 0.3.3`；schema `2 → 2`；requirement/development revision 3，router/review/documentation revision 1。保留 initializedAt，仅更新 lastUpdatedAt。
- 锁文件未记录本地清单自身版本，无受影响的内部包依赖引用，故无需改动锁元数据。相同变更集重试复用本基线/目标，不再次递增。

## 验收与审查

- 候选模板 revision、规则分支、当前版本事实、引用与托管边界检查通过；`git diff --check` 通过。
- SHA-256 基线检查确认用户的 kuVibe.md、DTO 改动、历史笔记、Vben active 产物、业务文档和锁文件均保留；仅合并项目版本约定及两个工作流。
- 产品清单仅改变自身 version；本次无应用运行时代码变化，不运行应用测试。
- 最终检查覆盖两份产品清单与 CHANGELOG、协议/安装状态、schema、模板 revision、初始化时间及文件保留范围。

## 文档影响、后果与后续

更新项目版本约定、工作流和 CHANGELOG；业务规则、数据模型、API/CLI、UI、运维及产品架构文档影响 N/A。以后完整需求默认评估并按规则实际递增，deferred 必须有证据和最终触发条件，完成总结始终报告版本结果。Vben 未完成里程碑和用户 DTO 改动不计入本次完成范围。本次无提交、打标签或发布；刷新无待办。

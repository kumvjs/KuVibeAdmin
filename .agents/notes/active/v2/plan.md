# v2 版本规划

状态：规划完成，实施未开始。当前初始化修复已完成，根与 core 均为 1.2.1。用户明确要求 v2 只规划。本目录保留为后续开发入口。

## 里程碑与出口

| 阶段 | 范围 | 前置条件与验收出口 |
| --- | --- | --- |
| V2-M0 范围冻结 | 以已有 Notification 组件核对交互，确认移除/清空语义、扩展范围、容量、保留策略、权限与部署范围 | requirement/analysis/acceptance 可评审；容量与产品决策落地；无实现 |
| V2-M1 个人消息闭环 | 内容与接收记录、个人列表/详情/未读/已读、内部发送服务 | M0 完成；越权隔离、并发与幂等测试通过；离线消息可补拉 |
| V2-M2 通知管理 | 草稿、接收对象预览、发布、接收者快照、投递汇总、审计 | M1 完成；发布独立授权；失败可追踪；大批量不阻塞请求 |
| V2-M3 可靠推送 | outbox 消费、退避重试、用户定向推送、多设备更新提示 | M1/M2 完成；断网/重启/重复消费不丢业务记录；多实例专项验证 |
| V2-M4 Vben 联调 | 复用现有 Notification 弹层接入列表、未读、已读、移除/清空；扩展 viewAll 与管理页，新增页面联调后才登记菜单 | M1–M3 完成；独立前端接入工程可用；浏览器及真实后端验收 |
| V2-M5 发布准备 | 清理与保留任务、监控、部署/回滚、迁移检查、安全及容量回归 | 全部阻塞项关闭；端到端验收与运维说明完整；再决定正式 2.x 发布 |

## 后续候选（不承诺进入首个 v2 闭环）

1. 运营审计检索：先核验已有日志模块覆盖，再补关键管理操作的查询、脱敏和保留策略。
2. 字典与运行配置：按真实业务需求建立权限、版本和缓存更新规则，不从 mock 生造表。
3. 异步任务运维：利用通知投递任务经验规划任务状态、重试与告警入口。
4. 多渠道消息、模板、定时公告、撤回与通知偏好：在站内投递稳定后评审。
5. AI Agents 业务仍独立待定义；本次不把 OpenAI SDK 依赖视作已完成产品能力。

## 本次交付与版本门禁

- [x] 读取本地源码、已验收版本与锁定 Vben 证据。
- [x] 将当前初始化修复与未来新增能力分开。
- [x] 记录需求、技术取舍、分阶段计划、验收及实施前待决策项。
- [ ] V2-M0 业务与容量冻结（未来实施前完成）。
- [ ] V2-M1–M5 实现和验收（用户未要求本次执行）。

v2 版本执行结果为 deferred：依据用户本次明确“v2 只做规划”；2.x 包版本递增延至正式实现和发布验收决策，不提前宣称应用。当前初始化修复按独立 patch 决策同步根与 core；KuVibe schema/revision 均不变。本规划不作为新功能已上线说明，也不重复建立 Swagger 接口手册。

## 分析修订记录

2026-09-18：用户指出布局已有 notifications 和通知组件。已核对 main 与锁定提交的布局，以及锁定提交的组件/类型，补充源码证据和事件映射。纠正仅查 API 契约造成的 UI 漏项，将生产架构建议与已知源码事实分开。本次只修订未实施 v2 规划，版本继续 deferred；根/core 均保持 1.2.1 → 1.2.1，待 v2 实现验收阶段决定递增。
